[uwsgi]
ini = /etc/uwsgi/base.ini

; Serves the mapped port at the root prefix, with no script name.
http-socket = 127.0.0.1:8002

processes = 1
threads = 4

; Spawn the worker on the first request. Users who only ever open the sidebar
; panel then pay for the master process and nothing else.
cheap = true
