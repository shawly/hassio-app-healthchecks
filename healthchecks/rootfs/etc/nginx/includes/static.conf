# Served by nginx rather than WhiteNoise: WhiteNoise reads settings.STATIC_URL
# once, when the middleware is constructed, and this app runs the app under
# two different URL prefixes.
alias /opt/healthchecks/static-collected/;
access_log off;
expires 1h;
add_header Cache-Control "public";
