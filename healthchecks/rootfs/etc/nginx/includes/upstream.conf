# Serves the app under the Home Assistant ingress path prefix.
upstream ingress_backend {
    server 127.0.0.1:8001;
}

# Serves the app at the root prefix on the mapped port.
upstream direct_backend {
    server 127.0.0.1:8002;
}
